/* ich werde jetzt ne funktion erstellen (are of a circle)
diese Funktion ist dann später mein npm package */

module.exports.area = radius => Math.PI*radius*radius;
module.exports.peri = radius => 2*Math.PI * radius;


/* Schritte um npn zu veröffentlichen 

npm login 
npm whoami
.. achte darauf dass in package.json dein name unique ist, bevor du espubliziert
npm publish

wenn es gepublished wurde muss ich ein readme kreieren (README.md);
danach im terminal mein npm aktualisieren auf versin 1.0.1 und wieder
publishen
*/
