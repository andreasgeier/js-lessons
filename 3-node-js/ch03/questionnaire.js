'use strict'

// Welches node.js Modul kann mit binären Daten umgehen?
// z.B: erzeugen zufälliger Binärzahlen
const buffer = require('buffer')

// Welches node.js Modul ermöglicht debugging, auch auf globalem Scope?
// z.B. das Loggen eines Prozesses
const console = require('console')

// Welches node.js Modul stellt kryptografische Funktionen bereit?
// z.B. Bereitstellen eines Zertifikats
const crypto = require('crypto')

// Welches node.js Modul ermöglicht Zugriff auf das Dateisystem?
// z.B. Schreiben und Löschen von Dateien
const fs = require('fs')

// Welches node.js Modul ermöglicht die Interaktion mit dem Betriebssystem?
// z.B. Abfrage der CPU Informationen
const os = require('os')

// Welches node.js Modul ermöglicht das portionsweise Versenden von Daten?
// z.B. Anfordern größerer Dateien
const stream = require('stream')

// Welches node.js Modul stellt Datenkomprimierung bereit?
// z.B. Packen und Entpacken von Dateien
const zlib = require('zlib')
